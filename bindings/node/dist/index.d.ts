export declare function isValid(plate: string, countryCode: string): Promise<boolean>;
export declare function isValidSync(plate: string, countryCode: string): Promise<boolean>;
declare const _default: {
    isValid: typeof isValid;
};
export default _default;
